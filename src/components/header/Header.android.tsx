import React from 'react';

import {Text} from 'react-native';

import HeaderStyle from 'styles/HeaderStyle';

const Header = () => {
  return <Text style={HeaderStyle.androidHeader}>Restaurant List</Text>;
};

export default Header;
